'use client';
import { motion, useReducedMotion } from 'framer-motion';
import { useState } from 'react';
const entries = [{ phase: 'Foundation', title: 'Enterprise payments', copy: 'Developing the operational discipline that makes financial systems reliable, resilient and useful.' }, { phase: 'Scale', title: 'Transformation leadership', copy: 'Bringing people, process and technology into a coherent operating rhythm.' }, { phase: 'Expand', title: 'Innovation platforms', copy: 'Applying enterprise insight to new models for value creation and service design.' }, { phase: 'Legacy', title: 'Journey to NewLand', copy: 'A continuing exploration of reinvention, generosity and work that outlives its first chapter.' }];
export function CareerTimeline() { const [active, setActive] = useState(0); const reduced = useReducedMotion(); const item = entries[active]; return <div className="mt-12"><div role="tablist" aria-label="Career timeline" className="grid gap-2 sm:grid-cols-4">{entries.map((entry, index) => <button role="tab" aria-selected={active === index} className={`focus-ring rounded-xl p-4 text-left transition ${active === index ? 'bg-navy text-white shadow-card' : 'bg-mist text-navy hover:bg-slate-200'}`} key={entry.phase} onClick={() => setActive(index)}><span className="block text-xs font-bold uppercase tracking-[.16em] text-gold">0{index + 1}</span><span className="mt-3 block font-display text-lg font-semibold">{entry.phase}</span></button>)}</div><motion.div key={item.phase} role="tabpanel" initial={{ opacity: 0, y: reduced ? 0 : 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .3 }} className="mt-5 rounded-2xl border border-slate-100 bg-white p-8 shadow-card"><p className="text-sm font-bold text-royal">{item.phase}</p><h2 className="mt-3 font-display text-3xl font-semibold text-navy">{item.title}</h2><p className="mt-4 max-w-2xl text-lg leading-8 text-slate-600">{item.copy}</p></motion.div></div>; }
