'use client';
import { motion, useReducedMotion } from 'framer-motion';
const moments = [{ number: '01', title: 'See the system', copy: 'Start with the real conditions: people, decisions, risk and customer need.' }, { number: '02', title: 'Create alignment', copy: 'Give teams a shared language for the change they are building together.' }, { number: '03', title: 'Build momentum', copy: 'Translate strategy into a steady sequence of visible, accountable progress.' }, { number: '04', title: 'Leave a legacy', copy: 'Design standards and capabilities that remain useful long after launch.' }];
export function ExecutiveTimeline() { const reduced = useReducedMotion(); return <ol id="story" className="mt-16 grid gap-5 md:grid-cols-4">{moments.map((moment, index) => <motion.li initial={{ opacity: 0, y: reduced ? 0 : 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .3 }} transition={{ duration: .5, delay: index * .1 }} className="relative border-t border-white/25 pt-6" key={moment.number}><span className="font-display text-4xl text-gold">{moment.number}</span><h3 className="mt-7 font-display text-xl font-semibold">{moment.title}</h3><p className="mt-3 text-sm leading-6 text-slate-300">{moment.copy}</p></motion.li>)}</ol>; }
