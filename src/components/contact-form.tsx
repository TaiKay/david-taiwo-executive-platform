'use client';
import { FormEvent, useState } from 'react';
import { reportEngagement } from '@/lib/analytics-events';
export function ContactForm() { const [status, setStatus] = useState(''); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get('name') ?? '').trim(); const email = String(data.get('email') ?? '').trim(); const message = String(data.get('message') ?? '').trim(); if (!name || !email || !message) return; reportEngagement('contact_compose'); const subject = encodeURIComponent(`Executive platform enquiry from ${name}`); const body = encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${message}`); setStatus('Opening your email application.'); window.location.href = `mailto:hello@davidtaiwo.com?subject=${subject}&body=${body}`; } return <form className="surface p-7" onSubmit={submit}><label className="block text-sm font-semibold" htmlFor="name">Name</label><input className="focus-ring mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 transition focus:border-royal" id="name" name="name" autoComplete="name" required/><label className="mt-5 block text-sm font-semibold" htmlFor="email">Email</label><input className="focus-ring mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 transition focus:border-royal" id="email" name="email" type="email" autoComplete="email" required/><label className="mt-5 block text-sm font-semibold" htmlFor="message">Message</label><textarea className="focus-ring mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 transition focus:border-royal" id="message" name="message" rows={5} required/><button className="action-primary mt-5" type="submit">Compose message</button>{status && <p aria-live="polite" className="mt-4 text-sm text-slate-600">{status}</p>}</form>; }
