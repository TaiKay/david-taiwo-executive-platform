'use client';
import Link from 'next/link';
import { AnimatePresence, motion } from 'framer-motion';
import { useState } from 'react';
const links = [{ href: '/dashboard', label: 'Dashboard' }, { href: '/portfolio', label: 'Innovation Lab' }, { href: '/speaking', label: 'Speaking' }, { href: '/opportunities', label: 'Opportunities' }, { href: '/contact', label: 'Contact' }];
export function Navigation() { const [open, setOpen] = useState(false); return <header className="glass-nav sticky top-0 z-50"><nav aria-label="Primary" className="mx-auto flex max-w-7xl items-center justify-between px-5 py-3.5 sm:px-8"><Link className="focus-ring font-display text-lg font-bold tracking-tight text-navy" href="/">DAVID <span className="text-gold">TAIWO</span></Link><button onClick={() => setOpen(!open)} className="focus-ring rounded-lg px-2 py-1 text-sm font-semibold text-navy md:hidden" aria-expanded={open} aria-controls="mobile-nav">{open ? 'Close' : 'Menu'}</button><div className="hidden items-center gap-6 lg:flex">{links.map((link) => <Link className="focus-ring text-sm font-medium text-slate-600 transition-colors hover:text-navy" href={link.href} key={link.href}>{link.label}</Link>)}<Link className="action-primary px-4 py-2 text-sm" href="/contact">Connect</Link></div></nav><AnimatePresence>{open && <motion.div id="mobile-nav" initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: 'auto' }} exit={{ opacity: 0, height: 0 }} transition={{ duration: .2 }} className="overflow-hidden border-t border-slate-100 bg-white/95 px-5 backdrop-blur-xl md:hidden">{links.map((link) => <Link onClick={() => setOpen(false)} className="focus-ring block border-b border-slate-100 py-3.5 font-medium text-navy last:border-0" href={link.href} key={link.href}>{link.label}</Link>)}</motion.div>}</AnimatePresence></header>; }
