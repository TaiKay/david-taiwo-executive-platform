'use client';
import Link from 'next/link';
import { useState } from 'react';
const links = [{ href: '/dashboard', label: 'Dashboard' }, { href: '/portfolio', label: 'Innovation Lab' }, { href: '/speaking', label: 'Speaking' }, { href: '/opportunities', label: 'Opportunities' }, { href: '/contact', label: 'Contact' }];
export function Navigation() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-50 border-b border-slate-100 bg-white/90 backdrop-blur"><nav aria-label="Primary" className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4 sm:px-8"><Link className="focus-ring font-display text-lg font-bold tracking-tight text-navy" href="/">DAVID <span className="text-gold">TAIWO</span></Link><button onClick={() => setOpen(!open)} className="focus-ring rounded md:hidden" aria-expanded={open} aria-controls="mobile-nav">Menu</button><div className="hidden items-center gap-7 md:flex">{links.map((link) => <Link className="focus-ring text-sm font-medium text-slate-600 hover:text-navy" href={link.href} key={link.href}>{link.label}</Link>)}<Link className="focus-ring rounded-full bg-navy px-4 py-2 text-sm font-semibold text-white hover:bg-royal" href="/contact">Connect</Link></div></nav>{open && <div id="mobile-nav" className="border-t border-slate-100 bg-white px-6 py-4 md:hidden">{links.map((link) => <Link onClick={() => setOpen(false)} className="focus-ring block py-3 font-medium" href={link.href} key={link.href}>{link.label}</Link>)}</div>}</header>; }
