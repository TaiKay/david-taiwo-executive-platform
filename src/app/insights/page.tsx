import Link from 'next/link';
import { PageHero } from '@/components/page-hero';
const articles = [{ slug: 'operational-excellence', title: 'Operational excellence is a leadership practice', description: 'Why dependable systems begin with the choices leaders make every day.' }, { slug: 'payments-trust', title: 'Trust is the real payment rail', description: 'Designing payment experiences around confidence, not only convenience.' }];
export default function Insights() { return <><PageHero label="Publishing & media" title="Ideas for the work that matters." text="Writing on enterprise operations, payments, leadership and the disciplines that make innovation last."/><section className="section grid gap-5 md:grid-cols-2">{articles.map((article) => <Link className="focus-ring rounded-2xl border border-slate-100 p-8 shadow-card hover:border-gold" href={`/insights/${article.slug}`} key={article.slug}><p className="text-sm font-bold text-royal">Executive note</p><h2 className="mt-8 font-display text-2xl font-semibold text-navy">{article.title}</h2><p className="mt-3 leading-7 text-slate-600">{article.description}</p><span className="mt-8 inline-block font-semibold text-royal">Read article →</span></Link>)}</section></>; }
