import type { MetadataRoute } from 'next';
export default function sitemap(): MetadataRoute.Sitemap { const routes = ['', '/dashboard', '/about', '/leadership', '/resume', '/portfolio', '/case-studies', '/journey', '/insights', '/publications', '/speaking', '/opportunities', '/media-kit', '/contact', '/downloads']; return routes.map((route) => ({ url: `https://davidtaiwo.com${route}`, lastModified: new Date(), changeFrequency: 'monthly', priority: route === '' ? 1 : .7 })); }
