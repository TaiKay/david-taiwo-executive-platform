import { PageHero } from '@/components/page-hero';
const items = [['Clarity', 'Make the decision, direction and standard visible.'], ['Stewardship', 'Build for the next team, customer and generation.'], ['Courage', 'Address the difficult work with conviction and care.'], ['Momentum', 'Turn strategy into a rhythm of observable progress.']];
export default function Leadership() { return <><PageHero label="Leadership" title="High standards. Deep humanity." text="Leadership is the practice of creating the conditions in which people can do their most consequential work."/><section className="section grid gap-5 sm:grid-cols-2">{items.map(([title, copy]) => <article className="rounded-2xl border border-slate-100 p-8 shadow-card" key={title}><h2 className="font-display text-2xl font-semibold text-navy">{title}</h2><p className="mt-3 leading-7 text-slate-600">{copy}</p></article>)}</section></>; }
