import Link from 'next/link';
import { PageHero } from '@/components/page-hero';
import { StructuredData } from '@/components/structured-data';
const pieces = [{ title: 'Operational excellence is a leadership practice', href: '/insights/operational-excellence', type: 'Article', copy: 'Why dependable systems begin with leaders.' }, { title: 'Trust is the real payment rail', href: '/insights/payments-trust', type: 'Article', copy: 'Designing payment experiences around confidence, not only convenience.' }, { title: 'Journey to NewLand', href: '/journey', type: 'Book project', copy: 'An evolving body of reflection on reinvention and leadership.' }];
export default function Publications() { const data = pieces.map((piece) => ({ '@context': 'https://schema.org', '@type': piece.type === 'Article' ? 'Article' : 'Book', headline: piece.title, name: piece.title, author: { '@type': 'Person', name: 'David Taiwo' }, url: `https://davidtaiwo.com${piece.href}` })); return <><StructuredData data={data}/><PageHero label="Publications library" title="A library for the work ahead." text="Essays and long-form thinking on leadership, operational excellence, payments and becoming."/><section className="section grid gap-5 md:grid-cols-3">{pieces.map((piece) => <Link href={piece.href} className="focus-ring rounded-2xl border border-slate-100 p-7 shadow-card hover:border-gold" key={piece.title}><p className="text-sm font-bold text-royal">{piece.type}</p><h2 className="mt-10 font-display text-2xl font-semibold text-navy">{piece.title}</h2><p className="mt-4 leading-7 text-slate-600">{piece.copy}</p><span className="mt-8 inline-block font-semibold text-royal">Explore →</span></Link>)}</section></>; }
