import OpenAI from 'openai';
import { NextResponse } from 'next/server';

export const runtime = 'edge';
const system = `You are David Taiwo's executive platform assistant. Answer concisely and professionally using only this scope: David Taiwo is a Senior Banking Operations & Enterprise Payments Executive focused on leadership, innovation and legacy. Topics include enterprise payments, operational transformation, MTN DataPay, Enterprise CDA, HomeCare, VBAN Store and Journey to NewLand. For opportunities, direct users to hello@davidtaiwo.com. Do not invent career history, qualifications, commercial claims or personal details.`;
export async function POST(request: Request) { const { message } = await request.json() as { message?: unknown }; if (typeof message !== 'string' || message.trim().length < 2 || message.length > 1000) return NextResponse.json({ error: 'Please enter a message between 2 and 1000 characters.' }, { status: 400 }); if (!process.env.OPENAI_API_KEY) return NextResponse.json({ error: 'The assistant is not configured.' }, { status: 503 }); const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY }); try { const completion = await client.chat.completions.create({ model: process.env.OPENAI_MODEL ?? 'gpt-4o-mini', messages: [{ role: 'system', content: system }, { role: 'user', content: message.trim() }], max_tokens: 350, temperature: .4 }); return NextResponse.json({ answer: completion.choices[0]?.message.content ?? 'I could not generate a response.' }); } catch { return NextResponse.json({ error: 'The assistant is temporarily unavailable. Please try again later.' }, { status: 502 }); } }
